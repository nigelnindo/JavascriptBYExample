// whilee loop does tha same thing as for loop
// Difference is you define the initializing variable outside

var x = 0;

//  Loops terminate once the specified boolean is a false
while (x <= 10) {
    console.log(x)
    x++;
}
